<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Rubyist Bot</title>
    <style type="text/css">
      tr:nth-child(odd) { background-color: #eef; }
    </style>
  </head>
  <body>
    <h1>Rubyist Bot</h1>
    <form method="POST" action="/submit">
      <table>
        <% tweets.each do |tweet| %><tr>
          <td>
            <input
              type="hidden"
              name="tweet[<%= tweet['status']['id'] %>][interesting]"
              value="<%= tweet['interesting'] %>"
              />
            <input
              type="checkbox"
              id="tweet-<%= tweet['status']['id'] %>"
              name="tweet[<%= tweet['status']['id'] %>][checked]"
              value="true"
              <%= %!checked="true"! if tweet['interesting'] %>
              />
          </td>
          <td>
            <label for="tweet-<%= tweet['status']['id'] %>">
              <%= tweet['status']['text'] %>
            </label>
          </td>
        </tr><% end %>
      </table>
      <input type="submit" value="Submit" />
    </form>
  </body>
</html>

<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Rubyist Bot</title>
    <style type="text/css">
      tr:nth-child(odd) { background-color: #eef; }
    </style>
  </head>
  <body>
    <h1>Rubyist Bot</h1>
    <form method="GET" action="/">
      <input type="search" name="q" value="<%=h query %>" />
    </form>
    <form method="POST" action="/submit">
      <table>
        <% statuses.each do |status| %><tr>
          <td>
            <input
              type="hidden"
              name="tweet[<%= status.id %>][interesting]"
              value="<%= status.interesting %>"
              />
            <input
              type="checkbox"
              id="tweet-<%= status.id %>"
              name="tweet[<%= status.id %>][checked]"
              value="true"
              <%= %!checked="true"! if status.interesting %>
              />
          </td>
          <td>
            <label for="tweet-<%= status.id %>">
              <%= status.text %>
            </label>
          </td>
        </tr><% end %>
      </table>
      <input type="submit" value="Submit" />
    </form>
  </body>
</html>
